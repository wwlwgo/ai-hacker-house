# 工程协同 Agent Relay：8 月 2 日单人实施计划

## 1. 今日目标

在约 5 小时的有效开发时间内，完成一个本地可运行、可交互、可讲解的“工程协同 Agent Relay”Demo。

今天不证明外部系统已经打通，不证明真实多 Agent 网络已经存在。今天要证明的是：**当不同角色的消息进入 Relay 后，系统可以受控地派发、汇聚、纠偏、审批并生成报告草稿。**

## 2. 今日最终交付

### 必须交付

1. 本地可启动的 Demo 页面，优先 Streamlit；若安装或运行受阻，改为静态 HTML 页面或 CLI 时间线。
2. 一个可逐步点击运行的 SC 管专项核查案例。
3. 一个完整的 Mock Relay 流程：

```text
事项创建
-> Round 1：两个专业节点并行提交证据
-> Human Adapter：人工中途纠偏
-> Round 2：定向澄清
-> 汇总建议结论
-> 总监批准
-> 专项汇报 Markdown 草稿
```

4. 页面或 CLI 中可看到：事项状态、消息时间线、角色状态、证据引用、人工操作和报告草稿。
5. 一份能在断网状态下跑通的 Mock 演示数据。

### 今天不做

- 第二场景的运行实现；安全日报只作为未来场景说明。
- 真实 Codex、TRAE、Hermes、飞书、钉钉、微信、邮件接入。
- 真实项目资料、真实人员信息、真实照片或 BIM 解析。
- SQLite、用户登录、权限系统、多机、云服务器。
- Word/PDF、电子签章、正式发送。
- 真实 LLM API 调用。

## 3. 产品边界

页面的定位是“协同指挥台”，不是聊天窗口，也不是正式工程管理系统。

观众必须一眼看懂五件事：

1. 当前事项是什么、在哪个轮次、处于什么状态。
2. 两个专业角色并行处理了什么。
3. 每个专业结论依据什么证据。
4. 人可以在中间纠偏、暂停或批准。
5. 只有总监批准后，才会出现报告草稿。

## 4. 今日技术方案

```text
Python 3.9+
  + JSON fixtures
  + dataclasses / 普通字典
  + 进程内 Mock Relay 状态机
  + Streamlit 本地观察面板（优先）
  + Markdown 报告草稿
```

所有专业节点均为确定性 Mock：它们按预置数据返回结构化消息，不调用外部模型。页面必须显式显示“Mock 节点 / 脱敏演示数据”。

## 5. 5 小时执行节奏

| 时间 | 阶段 | 完成标准 | 未完成时的决策 |
| --- | --- | --- | --- |
| T+0–20 分钟 | 环境与骨架 | 已创建 Python 虚拟环境；Streamlit 快速安装/导入验证未通过，已切换零依赖静态 HTML；建立目录与启动入口 | 已执行降级，不继续排障 |
| T+20–80 分钟 | Relay 主链 | 已用 fixtures 跑出完整消息序列、状态和报告草稿；CLI 与自动测试通过 | 不上数据库，不加第二个场景 |
| T+80–140 分钟 | 观察面板 | 已完成单页 Streamlit 指挥台：事项、角色、时间线、证据和状态均可见 | 不做动画、Tab 或筛选 |
| T+140–190 分钟 | 人工控制 | 已完成逐步操作：Round 1、人工纠偏、Round 2、总监批准与重置均已接入 Relay | 使用 fixture 中的预设纠偏文案 |
| T+190–220 分钟 | 演示打磨 | 已补充状态中文含义、人工转折标识、报告区“重新演示”入口和三张阶段截图 | 不增加新业务功能 |
| T+220–270 分钟 | 稳定性与演练 | 已完成三次隔离浏览器会话演练、黑盒验收、核心单元测试和 CLI 备份验证 | 停止加功能，只修 Demo 问题 |
| 最后 30 分钟 | 路演准备 | 打开本地页面、文档、CLI 备份；练 Pitch | 不再改动代码 |

## 6. 功能优先级

### P0：无论如何必须完成

- 从 `case.json` 读取 SC 管演示案例。
- 显示 `trace_id`、当前轮次、状态和 Mock 标识。
- 显示至少 8 条结构化消息。
- 显示两个专业节点的并行回复和证据。
- 触发一条 `human.correction`，使状态转为澄清。
- 运行第二轮并出现汇总建议。
- 点击总监批准后出现 Markdown 报告草稿。
- 能一键重置并重复演示。

### P1：主链稳定后再做

- 点击证据显示关联消息和来源说明。
- 显示“缺失证据/冲突”状态。
- 自动播放演示流程。
- 更清晰的角色轨道和事项轨迹线。
- 安全日报场景作为只读案例卡片。

### P2：只有提前完成才做

- 安全日报场景真正可运行。
- SQLite 审计回放。
- 真实 `codex exec` Adapter 试验。
- 导出报告或截图。

## 7. 最小演示操作

```text
1. 点击“重置案例”
2. 点击“运行 Round 1”
3. 查看两个专业 Agent 的证据和初步结论
4. 点击“人工纠偏”
5. 点击“运行 Round 2”
6. 查看汇总建议和证据包
7. 点击“总监批准并生成报告”
8. 展示报告草稿和完整时间线
```

## 8. 演示降级方案

| 问题 | 降级方式 |
| --- | --- |
| Streamlit 无法安装或启动 | 运行 CLI 时间线，打开 Markdown 报告草稿 |
| 网络/API 不可用 | 继续使用全部本地 Mock 数据 |
| 页面局部出错 | 用 CLI 展示状态、消息与报告，不临时修视觉细节 |
| 时间不足 | 删除报告预览以外的所有增强功能，保留主链 |

## 9. 今日完成判定

完成不以代码行数衡量。满足以下五条即视为成功：

- 能在本机从头跑完一条案例；
- 能展示 Agent 并行消息；
- 能展示人工中途纠偏；
- 能展示证据与最终结论的对应关系；
- 能展示人批准后才生成的报告草稿。

## 10. 明天后再做

完成今日 Demo 后，才考虑把现有工程 Skill 作为角色能力包接入、增加安全日报第二场景、接入真实 Agent Adapter 或开展多节点实验。

## 11. 第 1 步实施记录（2026-08-02）

- 已创建项目内 Python 虚拟环境：`.venv`。
- 初次验证时安装进程尚未有效完成，过早导入导致 `No module named streamlit`；后续复核确认 Streamlit 1.50.0 已安装并可导入。
- 已补充 `streamlit_app.py` 作为最小观察页入口，并以本地 HTTP 访问和浏览器渲染验证通过；静态 HTML 继续保留作降级方案。
- 最小启动入口：`web/index.html`。后续只在此入口上增加单页观察台，不引入 Tab、动画或额外页面。
- 已建立 `src/relay/` 与 `src/reporting/` 作为下一步的代码边界；当前不含 Relay 状态机、Mock 消息或报告生成实现。
- 冻结运行场景为“SC 管壁厚专项核查”。唯一演示链路为：重置案例 -> Round 1 并行证据 -> 人工纠偏 -> Round 2 定向澄清 -> 拟定结论 -> 总监批准 -> Markdown 报告草稿。
- 明确不实现：第二场景运行时、真实 LLM/API、SQLite、登录权限、多机云端、外部消息渠道、Word/PDF、电子签章、正式发送及重型 Agent 框架。

## 12. 第 2 步实施记录（2026-08-02）

- Relay 核心：`src/relay/models.py`、`src/relay/core.py`，采用进程内状态机和内存审计时间线。
- 确定性 Mock 节点：`src/relay/mock_nodes.py`，所有回复只读取 `demo-fixtures/sc-pipe-inspection/case.json` 的脱敏内容。
- Markdown 报告：`src/reporting/draft.py`，仅在 `human.approval` 后从已批准结论包生成草稿。
- CLI 演示：运行 `python3 scripts/run_demo.py`，可查看状态、轮次、13 条结构化消息、拟定结论、证据引用和报告草稿。
- 自动测试：运行 `python3 -m unittest discover -s tests -p 'test_*.py' -v`，覆盖完整主链、人工纠偏门槛、审批门槛、重置和消息契约。
- 本阶段未修改 Streamlit 或静态页面的交互逻辑；下一步才将已验证的 Relay 对象接入单页协同指挥台。

## 13. 第 3 步实施记录（2026-08-02）

- Streamlit 指挥台：`streamlit_app.py`，通过 `st.session_state` 保存唯一 Relay 实例，不在页面内复制状态机或另造 Mock 消息。
- 页面结构：顶部事项状态、左侧角色轨道、中部可展开审计时间线、右侧证据与结论、底部 Human Adapter 控制区。
- 演示控制：`重置案例`、`运行 Round 1`、`人工纠偏`、`运行 Round 2`、`总监批准并生成报告`；按钮按 Relay 状态启用，不能跳过人工门槛。
- 页面验证：`python3 tests/verify_streamlit_page.py` 在浏览器中依次完成全部五步操作，并验证状态、拟定结论、报告草稿和重置。
- 启动命令：`.venv/bin/python -m streamlit run streamlit_app.py`，访问 `http://localhost:8501`。
- 保留 `web/index.html` 作为静态降级入口；本阶段未增加第二场景、LLM、数据库、登录、多机或外部接口。

## 14. 第 4 步实施记录（2026-08-02）

- 页面打磨：状态卡同时显示状态码与中文含义；`[人工介入]`、`[待人工确认]` 前置显示；审批后可在报告区点击“重新演示”。
- 三次演练：运行 `python3 tests/rehearse_demo.py`，三个隔离浏览器会话均完成五步主链并恢复至 `NEW`。
- 验证：核心单元测试、`tests/verify_streamlit_page.py`、`tests/acceptance_review.py`、`scripts/run_demo.py` 均已通过。
- 截图：`/tmp/agent-relay-initial.png`、`/tmp/agent-relay-correction.png`、`/tmp/agent-relay-approved.png` 已完成视觉检查；移动端截图为 `/tmp/agent-relay-dashboard-mobile.png`。
- 路演资料：已更新 `docs/Demo-演示脚本.md`、`docs/Pitch-3分钟.md`，并新增 `docs/现场演示操作卡.md`。
