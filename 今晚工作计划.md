# Agent Relay MVP 今晚工作计划

## 0. 目的与完成标准

本仓库用于准备 2026 年 8 月 2 日 Ship it Sunday 黑客松的项目基础。项目暂定为：

> **工程协同 Agent Relay**：一个与具体 Agent、渠道和网络节点解耦的协同中继。它把跨角色工程事项转换为受轮次控制、证据驱动、可人工介入和确认的消息流程。

今晚的目标不是完成正式产品或外部平台接入，而是留下可运行、可讲解、可由现场队友接手的 MVP 基础。

今晚完成后应具备：

1. 一份可发给潜在队友的项目说明。
2. 一套固定的 MVP 边界、消息协议和状态机。
3. 一套可公开演示的脱敏 SC 管专项核查案例数据。
4. 一个本地可运行的 Mock 消息总线与 Demo 流程。
5. 一份现场 Demo 和 3 分钟路演脚本。

## 1. 核心假设

个人可继续使用 Codex、TRAE、Hermes 或人工方式处理本地资料。跨组织、跨专业协作时，参与方通过逻辑 Adapter 把任务、证据和结论转换为统一消息并进入 Relay。

```text
任意 Agent / 人工 / 渠道
  -> Ingress Adapter
  -> Relay Inbox
  -> 路由、轮次控制、状态与审计
  -> Egress Adapter
  -> 目标角色的 Agent / 人工 / 渠道
```

Relay 不取代总监、专业人员或正式签发责任。它自动化的是信息整理、任务分派、证据汇聚和草稿生成；高风险结论和正式对外交付保留 Human Adapter 确认。

## 2. MVP 边界

### 本次实现

- 本地单机运行；不依赖网络和外部 API。
- 使用 Mock Adapter 模拟不同角色的 Agent 或人工端点。
- 使用一个脱敏工程事项演示完整闭环。
- 支持结构化消息、证据引用、状态流转、人工中途纠偏和最终确认。
- 生成 Markdown 格式的专项检查汇报草稿。

### 不在本次实现范围内

- 微信、飞书、钉钉、邮箱、企业系统的真实接入。
- 真实多机部署、云端总线、认证授权和权限体系。
- 电子签章、正式 PDF、正式外发。
- 完整五方责任主体流程或完整 A2A 标准兼容。
- 自动签发整改通知、工程暂停令、复工令等正式文件。
- 使用真实项目原始材料作为公开演示数据。

## 3. 技术栈与系统架构

### 3.1 MVP 确定技术栈

| 层次 | 选择 | 为什么现在选它 | 明天是否必须 |
| --- | --- | --- | --- |
| 运行时 | Python 3.9+ | 当前机器已具备 Python 3.9；脚本、数据处理和 Agent 编排最直接 | 是 |
| 领域模型 | `dataclasses` + JSON Schema 风格字典 | 不引入 Pydantic 等额外依赖；消息契约可直接序列化、展示和测试 | 是 |
| Relay 核心 | 纯 Python 同步事件路由 | 一个事项最多两轮、角色数量少；无需消息中间件或异步框架 | 是 |
| 持久化与审计 | SQLite（Python 内置 `sqlite3`） | 零安装、单文件、支持按 `trace_id` 回放完整消息链 | 是 |
| Demo 数据 | JSON fixtures + 本地图片/附件说明 | 可脱敏、可版本控制、可在断网时稳定演示 | 是 |
| Mock Agent | Python 规则与预置结构化回复 | 先验证协议、轮次和状态机，不依赖外部模型 | 是 |
| 本地观察界面 | Streamlit；CLI 时间线为保底 | 快速呈现状态、消息、证据与人工操作；界面失败仍可用 CLI 演示 | 尽量 |
| 报告生成 | Markdown（标准库文件写入） | 先验证“已批准结论包 -> 正式草稿”；Word/PDF 留到后续 | 是 |
| 测试 | `unittest`（标准库） | 确保全流程在现场可重复跑通，无额外测试框架 | 是 |
| 可选真实节点 | `subprocess` 调用 `codex exec` | 当前已安装 Codex CLI；作为一个 Adapter 试验，不成为主链路依赖 | 否 |

> 当前机器确认具备 Python 3.9.6、SQLite 3.51.0 与 Codex CLI 0.146.0。MVP 的必需核心仅依赖 Python 标准库；Streamlit 是可选增强依赖。

### 3.2 MVP 本地架构

```text
                         Streamlit 观察面板（可选）
                                      |
                                      v
用户/Human Adapter --->  Agent Relay Core  <--- Mock / Codex CLI Adapter
                          |      |       |
                          |      |       +--> Report Agent -> Markdown 草稿
                          |      |
                          |      +----------> SQLite 审计库
                          |
                          +-----------------> demo fixtures（JSON + 证据说明）
```

模块职责：

```text
models.py         消息、事项、证据、人工决定的数据模型
bus.py            Inbox、路由、投递、审计与时间线查询
state_machine.py  两轮上限、状态迁移、缺失/冲突/终止判断
agents.py         专业 Mock Agent、汇总 Agent、报告 Agent
storage.py        SQLite 初始化与读写
report.py         从已批准结论包生成 Markdown 草稿
run_demo.py       一键运行完整案例；CLI 是最低展示保障
```

### 3.3 可扩展的多节点架构（不在本次实现）

未来不改变消息契约，只替换或增加 Adapter：

```text
远端 Codex / TRAE / Hermes / 人工
  -> 本地 Node Gateway（Adapter）
  -> HTTPS JSON / WebSocket / 文件收件箱
  -> Relay 服务
  -> SQLite（MVP）/ PostgreSQL（生产）
```

生产级扩展的候选组件仅作为方向，不在明天引入：

| 能力 | 后续候选 | 不在 MVP 引入的原因 |
| --- | --- | --- |
| API 网关 | FastAPI | 单机 Demo 不需要独立 HTTP 服务 |
| 异步队列 | Redis Streams / RabbitMQ | 当前两轮、少角色流程可由进程内路由完成 |
| 多节点数据库 | PostgreSQL | SQLite 足以保存单机审计链 |
| 远端安全 | HTTPS、节点身份、签名、最小化附件权限 | 需要组织授权和安全设计，不能现场假设 |
| 渠道接入 | 企业微信、飞书、钉钉、邮件 Adapter | 需要企业应用权限与回调配置 |

### 3.4 技术选择原则

1. **核心协议先于框架。** `Message`、`trace_id`、证据引用、状态和人工权限不能绑死在 Streamlit、FastAPI 或某个模型产品上。
2. **最小依赖优先。** 本地 Demo 即使没有网络、没有 API Key、没有新安装依赖，仍可跑通。
3. **界面与核心分离。** Streamlit 只消费 Relay 的审计数据；没有 UI 时，`run_demo.py` 仍可演示。
4. **Adapter 隔离厂商差异。** Relay 不直接依赖 Codex、TRAE、Hermes、飞书或微信的专有接口。
5. **正式责任保留人工节点。** 模型输出和 Adapter 回传都只能形成建议或草稿，不能绕开签发权限。

## 4. 主 Demo 场景

场景：业主对现场疑似 SC 管壁厚不足提出质疑，项目总监发起跨专业核查。

```text
业主问题进入
  -> Relay 建立事项并识别待确认对象
  -> 并行派发给综合监控与通信专业 Agent
  -> 两个专业 Agent 返回不同来源的证据和结论
  -> Human Adapter 中途补充：对象或标准需进一步确认
  -> Relay 发起一次定向澄清
  -> 汇总 Agent 生成建议结论与证据包
  -> 总监 Human Adapter 批准或退回
  -> 报告 Agent 生成 Markdown 汇报草稿
```

该场景只用脱敏、模拟的文字、图片说明和表格内容。重点展示任务分派、并行核查、人工纠偏、证据回溯和结论汇聚，而非自动识别 BIM 或真实工程文件。

## 5. 角色与权限

| 角色 | 主要职责 | 是否可形成最终结论 |
| --- | --- | --- |
| `owner_adapter` | 提交业主质疑或补充要求 | 否 |
| `supervisor_agent` | 创建事项、拆分任务、组织追问 | 否 |
| `integrated_monitoring_agent` | 提交现场测量与专业依据 | 否 |
| `communications_agent` | 提交通信专业核查和表格依据 | 否 |
| `reconciler_agent` | 汇聚事实、识别冲突、形成建议 | 否 |
| `director_human_adapter` | 观察、补充、暂停、退回、批准 | 是 |
| `report_agent` | 基于已批准结论生成报告草稿 | 否 |

## 6. 协作轮次与状态

最大自动交互轮次为两轮，避免 Agent 无限制对话。

```text
NEW
  -> DISPATCHED
  -> EVIDENCE_COLLECTING
  -> CLARIFICATION_REQUIRED (可选，仅一次)
  -> RECONCILED
  -> HUMAN_CONFIRMATION
  -> APPROVED -> REPORT_DRAFTED
  -> RETURNED / INSUFFICIENT_EVIDENCE
```

每轮结束时 Relay 检查：

- 必需角色是否回复；
- 是否包含规定的证据引用；
- 各专业结论是否冲突；
- 是否收到人工暂停、纠正或补充；
- 是否超过轮次上限。

人工可在任意阶段发送 `human.note`、`human.correction`、`human.evidence`、`human.pause` 或 `human.approval`。这些消息进入同一审计链，并可影响后续路由。

## 7. 最小消息契约

```json
{
  "message_id": "msg-001",
  "trace_id": "sc-pipe-001",
  "round": 1,
  "sender": {"role": "supervisor", "adapter": "mock"},
  "receiver": {"role": "integrated_monitoring", "adapter": "mock"},
  "event_type": "verification.requested",
  "payload": {"task": "核对现场管材类型、壁厚和适用依据"},
  "evidence_refs": [],
  "requires_human_approval": false,
  "status": "delivered",
  "timestamp": "2026-08-02T12:00:00+08:00"
}
```

消息类型初版包括：

```text
issue.created
verification.requested
evidence.submitted
clarification.requested
human.correction
conclusion.proposed
human.approval
report.drafted
```

## 8. 今晚任务顺序

### P0：必须完成

1. 建立仓库、`.gitignore` 和目录骨架。
2. 形成组队说明：痛点、假设、主场景、MVP、所需队友与分工。
3. 形成技术设计：架构图、角色、消息契约、状态机、人工控制。
4. 编写脱敏 Demo fixtures：问题、专业回复、证据说明、人工纠偏、最终结论。
5. 编写 Demo 演示脚本和 3 分钟路演稿。

### P1：尽量完成

1. 实现纯 Python 的模型、总线、状态机和 Mock Agent。
2. 实现 `python run_demo.py` 一键运行。
3. 用 JSONL 或 SQLite 记录完整消息时间线。
4. 实现 Markdown 报告草稿生成。
5. 为主流程写一个冒烟测试。

### P2：仅有余力再做

1. 做 Streamlit 时间线观察面板。
2. 预留真实 `codex exec` Adapter 接口，不作为必需依赖。
3. 补充远端 Hermes 节点和企业 IM Adapter 的未来架构说明。

## 9. 建议目录

```text
agent-relay-mvp/
├── README.md
├── run_demo.py
├── requirements.txt
├── app/
│   ├── models.py
│   ├── bus.py
│   ├── state_machine.py
│   ├── agents.py
│   ├── storage.py
│   └── report.py
├── demo-fixtures/
│   └── sc-pipe-inspection/
├── docs/
│   ├── 组队说明.md
│   ├── MVP-技术设计.md
│   ├── Demo-演示脚本.md
│   ├── Pitch-3分钟.md
│   └── 未来演进.md
└── tests/
    └── test_demo_flow.py
```

## 10. 明天现场的执行原则

1. 无论是否找到队友，先让本地 Demo 主流程跑通。
2. 不新增第二个业务场景，除非主场景已稳定完成。
3. 不为了漂亮 UI 牺牲消息、证据和人工确认主线。
4. 现场网络或 API 不可用时，切换到 Mock 模式。
5. 不展示真实项目名称、群成员、原始聊天记录、联系方式或受限资料。

## 11. 验收标准

今晚结束时，以下命令或材料至少应可用：

```text
git status
python run_demo.py
python -m unittest
```

以及以下文档：

- `docs/组队说明.md`
- `docs/MVP-技术设计.md`
- `docs/Demo-演示脚本.md`
- `docs/Pitch-3分钟.md`

如果 P1 因时间未完成，P0 文档和脱敏 fixtures 仍是明天可以直接与队友共同实现的完整规格。
